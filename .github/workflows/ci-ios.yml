name: iOS CI

on:
  push:
    branches: [main]
    paths:
      - 'receiver/**'
      - 'proto/**'
      - 'fixtures/**'
      - '.github/workflows/ci-ios.yml'
  pull_request:
    paths:
      - 'receiver/**'
      - 'proto/**'
      - 'fixtures/**'
      - '.github/workflows/ci-ios.yml'

jobs:
  # TODO: Enable when Xcode project is set up
  # ios-build:
  #   runs-on: macos-14
  #   ...
