name: iOS CI

on:
  push:
    branches: [main]
    paths:
      - 'receiver/**'
      - 'proto/**'
      - 'fixtures/**'
      - '.github/workflows/ci-ios.yml'
  pull_request:
    paths:
      - 'receiver/**'
      - 'proto/**'
      - 'fixtures/**'
      - '.github/workflows/ci-ios.yml'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "iOS CI disabled until Xcode project is set up"
